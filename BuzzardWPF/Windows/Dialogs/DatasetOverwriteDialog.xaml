<Window x:Class="BuzzardWPF.Windows.Dialogs.DatasetOverwriteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BuzzardWPF.Windows"
        Title="File Conflict Found"
        Width="425"
        Height="575"
        MinWidth="400"
        MinHeight="500"
        MaxWidth="700"
        ShowInTaskbar="False"
        SnapsToDevicePixels="True"
        Topmost="True"
        UseLayoutRounding="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="60" />
            <RowDefinition Height="60" />
            <RowDefinition Height="auto" MinHeight="22" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0"
                      Grid.RowSpan="2"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" MinHeight="100" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" MinHeight="100" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Margin="1"
                           FontSize="14"
                           FontWeight="Black"
                           Text="Item being renamed:" />
                <TextBlock Grid.Row="2"
                           Margin="1"
                           FontSize="14"
                           FontWeight="Black"
                           Text="Item being replaced:" />

                <local:FileFolderInfoViewer x:Name="m_sourcePathDataViewer"
                                            Grid.Row="1"
                                            Margin="1,2,1,20" />
                <local:FileFolderInfoViewer x:Name="m_destinationPathDataViewer"
                                            Grid.Row="3"
                                            Margin="1,2,1,20" />
            </Grid>
        </ScrollViewer>

        <Button Grid.Row="2"
                Click="Replace_Click"
                Content="Replace"
                Style="{StaticResource SimpleButtonStyle}" />
        <Button Grid.Row="3"
                Click="SkipDataset_Click"
                Content="Don't Rename"
                Style="{StaticResource SimpleButtonStyle}" />
        <CheckBox Grid.Row="4"
                  Margin="1,4,1,1"
                  Content="Do this for all Dataset rename conflicts."
                  FontSize="14"
                  IsChecked="{Binding Path=DoSameToOtherConflicts,
                                      Mode=TwoWay,
                                      UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</Window>
